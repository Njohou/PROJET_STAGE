import { MyDirectiveWithBackgroundImageDirective } from './my-directive-with-background-image.directive';

describe('MyDirectiveWithBackgroundImageDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDirectiveWithBackgroundImageDirective();
    expect(directive).toBeTruthy();
  });
});
